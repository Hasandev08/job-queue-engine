node_modules
.env

# Local-only learning journal — kept off GitHub by choice
docs/

# Editor/tool temp files
.claude/*.tmp*

# Claude Code local (per-user) settings — keep shared settings.json, ignore local overrides
.claude/settings.local.json
